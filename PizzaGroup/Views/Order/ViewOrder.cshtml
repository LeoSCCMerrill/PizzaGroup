@model Order
@ViewBag.Orders


@if (@Model == null || @Model.PizzaList.Count == 0)
{
<h1>Nothing is here</h1>
<form method="GET">
    <a asp-action="ListPizzas" asp-controller="Customer" class="btn btn-primary">Buy A Pizza</a>
</form>
}else{

<table style="border-width:1; border-color:" Black";>
    <thead>
        <tr>
            <th>Pizza ID</th>
            <th>Price</th>
            <th>Delivery/Pickup</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <form method="post">
            @foreach (Pizza pizza in @Model.PizzaList)
            {
            <tr>
                <td>@pizza.Id</td>
                <td>@pizza.Price</td>
                <td>
                    <input type="radio" name="delivery" /><label> Delivery</label>
                    <br />
                    <input type="radio" name="pickup" /><label> Pickup</label>
                </td>
                <td><button asp-action="Delete" asp-route-pizzaId="@pizza.Id" asp-route-index="@Model.PizzaList.IndexOf(pizza)">Delete</button></td>
                <br />
            </tr>
            }
        </form>
    </tbody>
    </table>
    <Text>Total = @ViewBag.Total;</Text>

<h3>Enter a Delivery Address</h3>
@*<form method="post" asp-controller="Order">*@
<label>Address: </label>
<input type="text" name="address" pattern="(?=\S*\s)(?=[^a-zA-Z]*[a-zA-Z])(?=\D*\d)[a-zA-Z\d\s',.#/-]*" required />
<br />
<label>Zipcode: </label>
<input type="text" name="zipcode" pattern="[0-9]{5}" required />
<br />
@*</form>*@

<form method="post" asp-action="SubmitOrder" asp-controller="Order">
    <button class="btn btn-primary" value="SUBMIT">Submit</button>
</form>
}
